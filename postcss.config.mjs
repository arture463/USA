/**
 * Configuration PostCSS — Tailwind CSS v3.
 * (En v3, Tailwind est un plugin PostCSS classique, contrairement à la v4.)
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
